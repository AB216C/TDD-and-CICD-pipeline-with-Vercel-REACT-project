{
    "version": 2,
    "buildCommand": "cd E-commerce-app && npm install && npm run build",
    "outputDirectory": "E-commerce-app/dist",
    "routes": [
        {
        "src": "/(.*)",
        "dest": "/index.html"
        }
    ]
}